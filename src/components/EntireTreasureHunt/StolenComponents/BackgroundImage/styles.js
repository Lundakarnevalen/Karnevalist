import { HEIGHT, WIDTH } from '~/src/helpers/Constants'

export const styles = {
  containerStyle: {
    position: 'absolute',
    width: WIDTH,
    height: HEIGHT
  },
  backgroundImageStyle: {
    width: WIDTH,
    height: HEIGHT,
    position: 'absolute'
  },
  opacityStyle: {
    backgroundColor: 'rgba(0, 0, 0, 0.3)',
    width: WIDTH,
    height: HEIGHT,
    position: 'absolute'
  }
}
